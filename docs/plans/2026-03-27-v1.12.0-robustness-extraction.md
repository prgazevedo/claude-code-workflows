# v1.12.0 Design: Robustness Fixes, Tooling Extraction, COMPLETE Pipeline Refactor

**Date:** 2026-03-27
**Version:** v1.12.0
**Issues:** #4488, #4570, #4581, #4586, #4590

## Problem

Five open issues spanning security false positives, tech debt, missing UX polish, personal tooling mixed into the plugin, and a structural enforcement gap in the COMPLETE pipeline:

1. **#4586 — FILE_OPS false positives:** `bash-write-guard.sh` FILE_OPS regex matches command names mid-word (e.g., `rm` inside `iTerm`), causing false blocks during legitimate COMPLETE phase operations like `gh issue edit`.

2. **#4570 — Three tech debt items:**
   - Worktree cleanup in COMPLETE Step 6 lacks merge-verification — can delete unmerged branches if push was deferred.
   - ~67 stale references to removed `save_completion_snapshot` across 5 historical design docs mislead future readers.
   - Unused `.claude/commands/` entry in `COMPLETE_WRITE_WHITELIST` — dead code.

3. **#4590 — Command output not reliably presented:** WFM command files use `!` backtick syntax but don't explicitly instruct Claude to present the output, unlike the cc-sessions pattern. Claude may silently consume the output.

4. **#4581 — YubiKey/iTerm tooling mixed into plugin repo:** Personal tooling (`tools/yubikey-setup/`, `tools/iterm-launcher/`) is committed to the WFM repo. These are user-specific, not plugin functionality.

5. **#4488 — COMPLETE Step 7 skipping:** Claude skips Steps 4-7 during COMPLETE, jumping from validation (Step 3) directly to commit (Step 5) or handover (Step 8). This has occurred three times in one session. Existing Check 9 coaching fires on specific tool patterns (git commit, save_observation) but can't detect step narration without execution.

## Approaches Considered

### FILE_OPS Fix (#4586)

**A: Word boundary `\b`** — `\brm[[:space:]]`. Clean but `\b` is PCRE/GNU, not portable ERE.

**B: Start-of-command anchor** — `(^|[;&|[:space:]])rm[[:space:]]`. Portable ERE, precise. More verbose.

**C: Accept false positives** — Guard is "a speed bump, not a wall." Live with the friction.

**Chosen: B.** Portable, precise, and the false positive on `gh issue edit` with "iTerm" in the body is a real usability problem.

### COMPLETE Step 7 Skipping (#4488)

**A: More milestone gates** — Add `tech_debt_audited`, `issues_created` fields. Extend Check 9 to verify before allowing commit. Same pattern as existing enforcement.

**B: Explicit step checklist** — Numbered checklist Claude must tick off. Visible but still relies on Claude following instructions.

**C: Structural pipeline** — Break Steps 4-8 into discrete agent dispatches. Each agent handles steps and sets milestones. Orchestrator can't skip because each step is a mandatory agent call with a required result.

**Chosen: C.** Options A and B add speed bumps to a problem that keeps recurring despite existing speed bumps. Check 9 was already supposed to catch this — it didn't. The review pipeline (5 parallel agents) proves the structural pattern works. Three sequential agents for Steps 4-8 provide real enforcement.

### Tooling Extraction (#4581)

**A: New separate repo** — Clean separation, version-controlled independently.

**B: Gitignored local overlay** — Co-located but not committed. Zero friction, no version control.

**C: Conditional blocks** — Guard behind env var. Least disruptive but adds conditionals.

**Chosen: A.** Separate repo at `azevedo-home-lab/claude-code-tools` (private). Files are moved (not just deleted), install URLs updated.

## Decision

- **Bundling strategy:** Approach C — single feature branch, stacked commits. One release cycle, granular git history for bisect.
- **Commit order:** Dependencies flow downward. Regex fix first (standalone), then tech debt, then command files, then extraction, then pipeline refactor, then docs-detector.

## Detailed Design

### Commit 1: FILE_OPS Word Boundary Fix (#4586)

**File:** `plugin/scripts/bash-write-guard.sh` line 26

Current:
```bash
FILE_OPS='(cp[[:space:]]|mv[[:space:]]|rm[[:space:]]|install[[:space:]]|patch[[:space:]]|ln[[:space:]]|touch[[:space:]]|truncate[[:space:]])'
```

Proposed:
```bash
FILE_OPS='((^|[;&|[:space:]])(cp|mv|rm|install|patch|ln|touch|truncate)[[:space:]])'
```

Each command must be preceded by line start, shell operator, or whitespace. Prevents matching inside words like `iTerm`, `perform`, `transform`.

**Tests to add** in `tests/run-tests.sh`:
- `gh issue edit` with "iTerm tooling" in body → ALLOW
- `rm file.txt` → DENY
- `echo foo && rm file.txt` → DENY
- `perform cleanup` → ALLOW
- `transform data` → ALLOW

### Commit 2: Remove Unused Whitelist Entry (#4570.3)

**File:** `plugin/scripts/workflow-state.sh` line 65

Remove `.claude/commands/` from `COMPLETE_WRITE_WHITELIST`:
```bash
# Before
COMPLETE_WRITE_WHITELIST='(\.claude/state/|\.claude/commands/|docs/|^[^/]*\.md$)'
# After
COMPLETE_WRITE_WHITELIST='(\.claude/state/|docs/|^[^/]*\.md$)'
```

### Commit 3: Merge-Verification Gate (#4570.1)

**File:** `plugin/commands/complete.md` Step 6 (around lines 309-311)

Before the `git worktree remove` and `git branch -d` instructions, insert:
```bash
# Verify branch was merged before cleanup
UNMERGED=$(git log origin/$MAIN_BRANCH..$CURRENT_BRANCH --oneline 2>/dev/null)
if [ -n "$UNMERGED" ]; then
    echo "WARNING: Branch has unmerged commits:"
    echo "$UNMERGED"
    echo "Proceed with cleanup anyway? (yes / no)"
fi
```

If unmerged commits exist, warn and re-prompt. Catches deferred push scenarios.

### Commit 4: Deprecation Notes (#4570.2)

Add a deprecation banner at the top of each affected file (after any existing frontmatter):

```markdown
> **Note (v1.11.0):** The `save_completion_snapshot` / `restore_completion_snapshot` mechanism referenced in this document was removed in v1.11.0. References below are historical.
```

**Files** (67 references across 5 files, excluding this spec):
- `docs/superpowers/specs/2026-03-23-open-issues-cleanup-design.md` (8 refs)
- `docs/superpowers/plans/2026-03-23-open-issues-cleanup.md` (21 refs)
- `docs/superpowers/plans/2026-03-23-tech-debt-cleanup.md` (9 refs)
- `docs/superpowers/specs/2026-03-27-security-fixes-architecture-cleanup-design.md` (8 refs)
- `docs/superpowers/plans/2026-03-27-security-fixes-architecture-cleanup.md` (19 refs)

### Commit 5: "Present the output to the user" (#4590)

Add `Present the output to the user.` after each `!` backtick block in all command files that produce user-relevant output. Follows the cc-sessions pattern.

**Files (9):**
- `plugin/commands/complete.md`
- `plugin/commands/implement.md`
- `plugin/commands/discuss.md`
- `plugin/commands/review.md`
- `plugin/commands/define.md`
- `plugin/commands/off.md`
- `plugin/commands/autonomy.md`
- `plugin/commands/obs-track.md`
- `plugin/commands/obs-untrack.md`

**Pattern:** Insert `Present the output to the user.` on the line immediately after the closing backtick, before any conditional checks or instructions.

### Commit 6: Extract YubiKey/iTerm Tooling (#4581)

**Target repo:** `azevedo-home-lab/claude-code-tools` (private)

**Phase 1 — Move files to new repo:**

Clone `claude-code-tools`, create structure:
```
claude-code-tools/
├── LICENSE                    # GPL-3.0-only
├── README.md                  # Overview + install instructions
├── yubikey-setup/             # Moved from tools/yubikey-setup/
│   ├── git-yubikey
│   ├── git-ssh-auth.sh
│   ├── git-ssh-sign.sh
│   ├── ssh-askpass-wrapper.sh
│   ├── install.sh             # Updated REPO_BASE URLs
│   ├── CLAUDE.md.snippet
│   └── README.md
├── iterm-launcher/            # Moved from tools/iterm-launcher/
│   ├── launch-claude-iterm.sh
│   ├── install.sh             # Updated REPO_BASE URLs
│   ├── claude-code-profile.json
│   └── README.md
└── tests/
    └── run-tests.sh           # git-yubikey tests moved here
```

**Install script URL updates:** All `REPO_BASE` references change from:
```
https://raw.githubusercontent.com/prgazevedo/claude-code-workflows/main/tools/yubikey-setup
```
to:
```
https://raw.githubusercontent.com/azevedo-home-lab/claude-code-tools/main/yubikey-setup
```
Same for iterm-launcher.

**Phase 2 — Clean up this repo:**

1. Delete `tools/yubikey-setup/` and `tools/iterm-launcher/`
2. Remove git-yubikey test suite from `tests/run-tests.sh` (~lines 2440-2520)
3. Update `README.md` — replace optional tools table with pointer to new repo
4. Update `complete.md` Step 5 push section — replace YubiKey-specific warning with generic push confirmation
5. Verify `install.sh` has no remaining YubiKey/iTerm references (current install.sh has no `--yubikey`/`--iterm` flags — this is a verification step, not a code change)
6. Leave historical docs untouched — they're design records, not living references

### Commit 7: COMPLETE Pipeline Agent Refactor (#4488)

Replace COMPLETE Steps 4-8 (currently inline instructions) with 3 sequential agent dispatches.

**New agent files:**

#### `plugin/agents/commit-push-agent.md`
**Scope:** Steps 4-6 (docs detection, commit/push, branch integration/worktree cleanup)
**Inputs:** Decision record path, changed files list, autonomy level
**Outputs:** Commit hash, push status, branch integration status
**Milestones set:** `docs_checked`, `committed`, `pushed`
**Review gates included:** docs-reviewer, commit-reviewer (dispatched within the agent)
**User interaction:** Push confirmation routed through structured response — agent returns `PUSH_PENDING` if confirmation needed, orchestrator relays to user and feeds answer back.

#### `plugin/agents/tech-debt-agent.md`
**Scope:** Step 7 (review tracked observations, collect/categorize findings, save observations, create GitHub issues, temp file cleanup)
**Inputs:** Decision record path, tracked observations list, validation findings from Steps 1-3
**Outputs:** Category observations saved, GitHub issues created, KEEP_IDS list, new observation IDs
**Milestones set:** `tech_debt_audited`
**Review gates included:** tech-debt-reviewer (dispatched within the agent)
**Shared state:** Writes KEEP_IDS and new observation IDs to `.claude/tmp/tech-debt-handoff.json` for the handover agent.

#### `plugin/agents/handover-agent.md`
**Scope:** Step 8 (write handover observation, update tracked observations list)
**Inputs:** Decision record path, `.claude/tmp/tech-debt-handoff.json` (from tech-debt-agent)
**Outputs:** Handover observation ID
**Milestones set:** `handover_saved`
**Review gates included:** handover-reviewer (dispatched within the agent)
**Wraps existing agent:** This agent orchestrates the existing `handover-writer.md` agent (which does the actual writing) and the `handover-reviewer.md` agent (review gate). It does NOT replace them — it adds the tracked observations update and milestone setting around them.
**Atomic update:** Builds final tracked observations list (KEEP_IDS + handover ID + new tech debt IDs) in a single `set_tracked_observations` call.

**Changes to `complete.md`:**

Steps 4-8 replaced with:
```markdown
### Steps 4-6: Commit Pipeline
Dispatch commit-push-agent...
Wait for result. Handle push confirmation if needed.

### Step 7: Tech Debt Audit
Dispatch tech-debt-agent...
Wait for result.

### Step 8: Handover
Dispatch handover-agent...
Wait for result.
```

Steps 1-3 remain unchanged — they already use agent dispatches and haven't had skipping issues.

**Check 9 updates in `post-tool-navigator.sh`:** The existing Check 9 coaching for COMPLETE (git commit before results_presented, git push before committed, save_observation before tech_debt_audited) remains as defense-in-depth. Even though agents handle the flow now, the coaching catches edge cases where the orchestrator attempts to act outside the agent pipeline.

### Commit 8: Docs-Detector Stale Reference Detection (feedback)

**File:** `plugin/agents/docs-detector.md`

Add a "Stale Reference Detection" section:

```markdown
## Stale Reference Detection

Scan documentation files for references to removed or renamed code artifacts:
- Function/variable names that no longer exist in the codebase
- File paths that no longer exist
- API endpoints that were removed
- Configuration keys that were deprecated

For each stale reference found, report:
| File | Line | Stale Reference | Status |
|------|------|-----------------|--------|
| <file> | <line> | <reference> | Removed in <version> |

Recommend either a deprecation note or content update depending on whether
the document is historical (specs/plans) or living (README, guides).
```

**Scope:** Only scans docs in the changed files list or adjacent (same directory). Does not sweep the entire docs/ tree — that's the cleanup agent's job.

## Trade-offs Accepted

1. **More tokens per COMPLETE run** (~30-40% more for Steps 4-8) due to 3 agent context windows instead of inline execution. This is the cost of structural enforcement over behavioral enforcement.
2. **Tooling gap during extraction** — between deletion from this repo and new repo setup, install URLs will 404. Acceptable because user controls timing and the new repo is created first.
3. **Stale reference detection is scoped** — only catches references near changed files, not repo-wide. Full sweeps deferred to the cleanup agent concept.
4. **FILE_OPS regex is more verbose** — harder to read at a glance, but the variables are already documented and the pattern is correct.

## Tech Debt Acknowledged

- Historical docs still contain 150+ snapshot references (now with deprecation banners, not rewritten)
- Check 9 coaching in post-tool-navigator.sh may become partially redundant after agent pipeline — keep as defense-in-depth for now, evaluate removal in future version
- The commit-push-agent handles user interaction (push confirmation) via structured response routing — this is a new pattern that may need refinement based on real usage

## Verification

Each commit has specific test criteria:
1. FILE_OPS: new test cases pass, existing tests unchanged
2. Whitelist: existing COMPLETE phase tests pass
3. Merge-verification: manual verification (instruction change only)
4. Deprecation notes: visual verification (doc banners)
5. Present output: manual verification (instruction change only)
6. Extraction: git-yubikey tests pass in new repo, removed from this repo, install URLs updated
7. Agent pipeline: COMPLETE pipeline runs end-to-end with all 8 milestones set
8. Docs-detector: agent file updated, manual verification of detection scope


## Decision Record (Archived)

# Decision Record: v1.12.0 — Robustness Fixes, Tooling Extraction, COMPLETE Pipeline Refactor

**Date:** 2026-03-27
**Version:** v1.12.0
**Spec:** `docs/superpowers/specs/2026-03-27-v1.12.0-robustness-extraction-design.md`

## Problem

Five open issues requiring a combined release:
- #4586: FILE_OPS regex false positives (mid-word matching)
- #4570: Three tech debt items (merge-verification, stale refs, unused whitelist)
- #4590: Command output not reliably presented to user
- #4581: YubiKey/iTerm personal tooling mixed into plugin repo
- #4488: COMPLETE Step 7 repeatedly skipped due to behavioral enforcement limits

## Approaches Considered (DISCUSS phase — diverge)

### FILE_OPS Fix
- **A: `\b` word boundary** — Clean but non-portable ERE
- **B: Start-of-command anchor** — `(^|[;&|[:space:]])cmd[[:space:]]` — portable, precise
- **C: Accept false positives** — Live with friction

### COMPLETE Pipeline
- **A: More milestone gates** — Same class of enforcement already failing
- **B: Explicit checklist** — Still relies on Claude following instructions
- **C: Structural agent pipeline** — Discrete dispatches, can't skip

### Tooling Extraction
- **A: Separate repo** — Clean separation, version controlled
- **B: Gitignored overlay** — Co-located, no version control
- **C: Conditional blocks** — Env var gating, adds complexity

### Bundling Strategy
- **A: Single release** — One batch, larger blast radius
- **B: Two micro-releases** — Smaller chunks, two cycles
- **C: Feature branch with stacked commits** — One cycle, granular history

## Decision (DISCUSS phase — converge)

- **FILE_OPS:** Approach B — start-of-command anchor. Portable and precise.
- **COMPLETE Pipeline:** Approach C — structural agent pipeline. Three sequential agents replace inline Steps 4-8.
- **Tooling Extraction:** Approach A — separate repo at `azevedo-home-lab/claude-code-tools` (private).
- **Bundling:** Approach C — feature branch, stacked commits, one release.

**Rationale:**
- FILE_OPS: The false positive on `gh issue edit` with "iTerm" blocked real work. Portable ERE is correct even if more verbose.
- COMPLETE: Check 9 coaching was already supposed to prevent step skipping — it didn't. Adding more milestone fields treats the symptom. The review pipeline proves agent-based structural enforcement works.
- Tooling: User-requested. Separate repo gives version control and clean separation.
- Bundling: Single cycle avoids overhead. Stacked commits give rollback granularity.

**Trade-offs accepted:**
- ~30-40% more tokens per COMPLETE run (3 agent windows vs inline)
- Brief tooling URL gap during extraction (user controls timing)
- Stale reference detection scoped to changed files only (full sweep deferred)
- Historical docs get deprecation banners, not content rewrites

**Risks identified:**
- commit-push-agent user interaction routing (push confirmation) is a new pattern
- Agent pipeline adds latency to COMPLETE phase
- FILE_OPS regex change could miss edge cases in command parsing

**Constraints applied:**
- ERE portability requirement (macOS grep -E)
- Sequential agent execution (Steps 4-8 have data dependencies)
- Historical docs preserved as design records

**Tech debt acknowledged:**
- Check 9 coaching partially redundant after agent pipeline (keep as defense-in-depth)
- 150+ snapshot references now bannered, not rewritten

**Additional enhancement from discussion:**
- Docs-detector agent gets stale reference detection capability
- This was user feedback: deprecation should be a regular concern for the documentation agent

## Outcome Verification (COMPLETE phase)
_To be filled during COMPLETE phase._

## Review Findings
_To be filled during REVIEW phase._
